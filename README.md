# Sudoku solver

A brute force Sudoku solver

## Usage

1) Run the application in a CLI (`java -jar sudoku-solver.jar`)
2) Enter your sudoku row wise, separated by commas (for empty fields enter 0)
3) If there is any solution, it gets outputted